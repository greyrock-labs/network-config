# 2026-07-18/19 — game-room-icx-7150 and garage-icx-7150 cut over to the spine

Fourth and fifth ICX leaves, same pattern as the 8200s with one
structural difference: **on the 7150s the uplink is 1/3/2** (the second
SFP+), not the port the old daisy chain used — 1/3/1 goes unused.

- game-room-icx-7150 → 10.1.0.15, uplink 1/3/2 `GameRoom-CRS309`.
  The Z-Wave device is gone: port 1/1/3 returned to default and removed
  from VLAN 10 (leaves SolarEdge and Zigbee bridges).
- garage-icx-7150 → 10.1.0.18, uplink 1/3/2 `Garage-CRS309`.
  New camera added same day: 1/1/5 `Side-Yard-Rear`, VLAN 10 untagged,
  BPDU-guarded like its siblings.
- Both: leaf STP priority 36864, multicast passive (IPv4-only
  flood-unregistered on this platform, as the June change intended).
- Unleashed provisioning lags the CLI work by a few minutes — the
  syslog and SNMP lines appear when it finishes; capture after it
  settles. The rotated SNMP community is visible in the new captures
  (scrubbed as always).

Remaining: office-icx-7150.