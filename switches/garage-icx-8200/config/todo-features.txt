! todo-features.txt — garage-icx-8200
!
! These are the next things to apply, captured from the user's notes
! during the initial setup. Each is independent and can be applied
! separately. We've not yet run any of them.

! --- RSTP priority / IGMP+MLD snooping / fast-convergence --------------
! (User-provided example; verify against `?` before pasting.)
!
! enable
! configure terminal
! ipv6 mld version 2
! vlan 1
! multicast fast-convergence
! multicast version 3
! multicast6 active
! multicast6 fast-convergence
! vlan 10
! multicast fast-convergence
! multicast version 3
! multicast6 active
! multicast6 fast-convergence
! exit
! write memory

! --- Dynamic PoE allocation --------------------------------------------
! (User-provided example; the ICX 8200 supports this — the actual
!  command may be `inline power allocation-mode dynamic` or similar.
!  Verify with `inline power ?` first.)
!
! enable
! configure terminal
! show poe
! inline power allocation dynamic all
! show poe
! write memory

! --- Jumbo frames ------------------------------------------------------
! (User wants this. Syntax: `jumbo` at the global config level.
!  Verify with `jumbo ?` first.)
